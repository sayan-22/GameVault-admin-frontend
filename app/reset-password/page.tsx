import ResetPasswordView from "@/src/views/auth/ResetPasswordView";

export const metadata = {
  title: "Reset password · GameVault Admin",
};

export default function ResetPasswordPage() {
  return <ResetPasswordView />;
}
